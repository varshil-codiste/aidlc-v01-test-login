# AI-DLC Profile — Preset Selection

This is your first AI-DLC project in this workspace. Before Stage 1 (Business Requirements) starts, we need to populate `aidlc-profile.md` so downstream stages know your team's defaults (pod roles, stack recommendations, ops preferences).

The workflow will refuse to advance to Gate #1 until at minimum `team.name`, `pod.roles`, `project.name`, and `project.workspace_root` are filled.

---

## Question 1
Would you like to apply a preset to fill the profile, or fill it from scratch?

A) Apply the `codiste` preset (40-person AI agency, full-stack FE/BE/Mobile — sensible defaults for Codiste team learning experiments) **(Recommended — your email is `@codiste.com`)**
B) Fill the profile from scratch (I'll ask you one section at a time via question files)
X) Other (please describe after [Answer]: tag below)

[Answer]:A

---

## Question 2
Who are the two pod members for this learning experiment? Both names are required before Gate #1 can be signed. (Fill in the role you currently hold and a second signer; if you're alone for the experiment, type the same name twice or list a stand-in.)

| Role | Name | Email / Handle |
|------|------|----------------|
| Tech Lead | [Chintan]: | [Chintanp@codiste.com]: |
| Dev       | [Varshil]: | [varshil.g@codiste.in]: |

---

## Question 3
What is the **project name** (used in artifact headers and as the prefix for `aidlc-state.md`'s Project field)?

A) `login-account-setup` (short kebab — recommended for a small learning experiment)
B) `aidlc-v01-test-login` (matches the existing repo directory name)
C) Free-form name — type below after `[Answer]:`
X) Other (please describe after [Answer]: tag below)

[Answer]:A

---

When you've filled in all three, say "done" (or "continue") and I'll apply the preset, finalize `aidlc-profile.md` + `pod.md`, then start Stage 1 — Business Requirements Intake.
